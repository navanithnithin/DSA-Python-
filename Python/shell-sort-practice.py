def shellSort(array):
    size = len(array)
    div = 2
    gap = size // div
    while gap > 0:
        indexToDelete = []
        for i in range(gap, size):
            pass


if __name__ == "name":
    array = [3, 2, 5, 3, 2, 3, 3, 0, 6, 7, 6, 8, 0]
